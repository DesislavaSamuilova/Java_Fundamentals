import java.util.Scanner;

public class DecryptingMessage_05 {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int key = Integer.parseInt(scanner.nextLine());
        int followingLines = Integer.parseInt(scanner.nextLine());
        String sumMessage = "";

        for (int i = 0; i < followingLines; i++) {

          //  char alphabetCharacter = scanner;
       //     int value = alphabetCharacter + key;

        //    char message = (char) value;
            if (i ==0) {
          //      message =
            }
        //    sumMessage += message;
        }
        System.out.println(sumMessage);

    }
}
